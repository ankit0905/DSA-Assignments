#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{	
	char text[102];
	scanf("%s",text);		
	int n;
	scanf("%d",&n);
	char word[n][50];
	for(i=0; i<n; i++){
		scanf(" %s",word[i]);
	}
	int len = strlen(text);
	for(i=0; i<len; i++){
		for(j=i; j<len; j++){
			
		}
	}
	return 0;
}
